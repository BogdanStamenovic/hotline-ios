.venv/
__pycache__/
*.pyc
.env
build/
*.ipa
*.app/
server/.venv/
.pytest_cache/

# SwiftPM build output and xtool packaging output. .gitignore already had
# "build/", which never matches ".build/" -- 1840 object files and dSYMs went
# in before that was noticed.
.build/
app/HotlineCall/xtool/

# Generated on the CI runner by xcodegen, never checked in.
ci/HotlineCall.xcodeproj/
ci/Info.plist
DerivedData/
out/
